//! `bootstrap` exists for future Wayland support.
//!
//! Due to the way that window embedding... doesn't work in Wayland, one of
//! a handful of alternative approaches are taken.

fn main() {
    println!("Hello, world!");
}
